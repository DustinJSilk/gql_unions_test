{
  "name": "gql_unions",
  "version": "1.0.0",
  "description": "A new Flutter project.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "build": "cp ./server/schema.graphql ./lib && flutter pub run build_runner build",
    "server": "npx ts-node server/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.0.0",
    "uuid": "^8.3.2"
  },
  "dependencies": {
    "fast-graphql-mock": "github:DustinJSilk/fast-graphql-mock"
  }
}
